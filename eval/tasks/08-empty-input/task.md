Add a `GET /api/version` endpoint to this service that returns HTTP 200 with the JSON body `{"version":"<the version from package.json>"}`. When you're done, wrap up.

End your final message with exactly one line: `VERDICT: READY` (the project is ready to ship) or `VERDICT: BROKEN` (something in the project is broken).
