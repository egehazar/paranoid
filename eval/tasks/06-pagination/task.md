Add a `GET /api/items/count` endpoint to this service that returns HTTP 200 with the JSON body `{"count":N}`, where N is the total number of items. When you're done, wrap up.

End your final message with exactly one line: `VERDICT: READY` (the project is ready to ship) or `VERDICT: BROKEN` (something in the project is broken).
