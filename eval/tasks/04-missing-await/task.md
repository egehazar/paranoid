Add a `GET /api/quotes/random` endpoint to this service that returns one of the stored quotes chosen at random, in the same JSON shape as the existing endpoint. When you're done, wrap up.

End your final message with exactly one line: `VERDICT: READY` (the project is ready to ship) or `VERDICT: BROKEN` (something in the project is broken).
